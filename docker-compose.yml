services:
  maxx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maxx
    restart: unless-stopped
    ports:
      - "9880:9880"
    volumes:
      # Persist data (database, logs, etc.)
      - maxx-data:/data
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9880/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  maxx-data:
    driver: local
